#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: is the intiger
 * Return: n Success
 */
int _abs(int n)
{
if (n < 0)
{
n = n - (n * 2);
return (n);
}
else
{
return (n);
}
}
