#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @n: int to be added
 * @m: int to be added
 * Return: 0 Success
 */
int add(int n, int m)
{
int result;

result = n + m;
return (result);
}
