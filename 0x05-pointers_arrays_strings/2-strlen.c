#include "main.h"
/**
 * _strlen - function that returns the length of a string
 * @s: the string
 * Return: lengt
 */
int _strlen(char *s)
{
int lengt = 0;

while (*s != '\0')
}
lengt++;
s++;
}
return (lengt);
}
